# Fedora WSL2 Bash entrypoint.

source "${HOME}/.config/bash/shell"
source "${HOME}/.config/bash/envs"
source "${HOME}/.config/bash/functions"
source "${HOME}/.config/bash/aliases"
source "${HOME}/.config/bash/init"
