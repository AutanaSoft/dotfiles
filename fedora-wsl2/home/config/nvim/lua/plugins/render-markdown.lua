-------------------------------------------------------------------------------
-- Render Markdown
-------------------------------------------------------------------------------
-- Keep the plugin installed but disabled until rendered Markdown is needed.
return {
  {
    'MeanderingProgrammer/render-markdown.nvim',
    opts = {
      enabled = false,
    },
  },
}
